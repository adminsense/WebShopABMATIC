@* Staff may browse the public store (Open webshop from admin; Admin link in store header).
   Post-login still sends Admin/Manager to /admin via /account/admin-login — do not bounce them off /. *@

@code {
}
